
The application includes 2 variants from implementation of the persistent expense manager.

1 persistentExpenseManager has been set default and it uses databaseAccountDAO and databaseTransactionDAO




2 persistentDemoExpenseManager is an optional implementation which uses persistentAccountDAO, persistentTransactionDAO and DatabaseHandler.

    The implementation includes a singleton database accessor class
